import 'package:flutter/material.dart';

class TermesWidget extends StatelessWidget {
  const TermesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "Termes Screen Widget",
        ),
      ),
    );
  }
}
